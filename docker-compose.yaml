version: '3.8'

services:
  postgres:
    image: postgres:18-alpine
    container_name: express_app_postgres_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: express_app
    ports:
      - "5440:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    networks:
      - db_network

  redis:
    image: redis:7-alpine
    container_name: express_app_redis_db
    restart: unless-stopped
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data


  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin_ui
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: adminpassword
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - postgres
    networks:
      - db_network

volumes:
  postgres_data:
  pgadmin_data:
  redis_data:

networks:
  db_network:
    driver: bridge